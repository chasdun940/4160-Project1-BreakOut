#IMPORTS
import pygame

# Paddle vars
paddleColor = (255, 0, 0)
paddleSize = padWidth, padHeight = 100, 10
paddlePos = padX, padY = 100, 550
paddleSpd = 8
paddle = pygame.Rect(padX, padY, padWidth, padHeight)